services:

  dicom_server:
    build: ./server
    container_name: dicom_server
    ports:
      - "11112:11112"
    volumes:
      - ./data:/data

  dicom_client:
    build: ./client
    container_name: dicom_client
    depends_on:
      - dicom_server